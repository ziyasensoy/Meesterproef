import SiteControls from "@/components/journey/SiteControls";

/** Sound + fullscreen - only rendered on the intro pin section. */
export default function IntroControls() {
  return <SiteControls />;
}
