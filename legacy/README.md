# Legacy static page

Original single-file HTML used as the **markup source** for `src/App.jsx`.

It is not served by Vite. To preview the live app, run `npm run dev` from the project root.

To refresh React markup after editing this file:

```bash
node scripts/convert-html.mjs
```
